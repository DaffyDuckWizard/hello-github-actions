name: "Hello Actions"
description: "Greet someone"
author: "octocat@github.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: false
    default: "World"
  
runs:
  using: composite
  steps:
    - run: echo line 1
      shell: cmd
    - run: echo line 2
      shell: cmd

  

branding:
  icon: "mic"
  color: "purple"